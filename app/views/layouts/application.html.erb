<!DOCTYPE html>
<html class="h-100 mt-0">
  <head>
    <title>Chamazon</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <nav class="navbar navbar-dark navbar-expand-lg bg-primary fixed-top">
      <div class="container-fluid">
    <%= link_to 'Chamazon', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to 'Accueil', root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Mon compte
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%if current_user.nil? %>
                <li><%= link_to 'Se connecter', new_user_session_path, class: 'dropdown-item' %></li>
                <li><%= link_to "Créer un compte", new_user_registration_path, class: 'dropdown-item' %></li>
                <%else%>
                <li><a class="dropdown-item" href="#">Mon compte</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><%= link_to "Se déconnecter", destroy_user_session_path, class: 'dropdown-item', method: "DELETE" %></li>
              <%end%>
              </ul>

            </li>
      </ul>
      <%if current_user%>
              <ul class="navbar-nav mr-0">
                <% if @items_in_cart && @items_in_cart.count > 0 %>
                  <% total_items = []
                      @items_in_cart.each do |item_choosed|
                          item_quantity = item_choosed.quantity
                          total_items << item_quantity
                      end %>
                <li class="nav-item"><%= link_to "Mon panier", cart_path(current_user.cart.id), class: "nav-link" %></li>
                    <span class="badge badge-black pt-4"><%= total_items.inject(0){|sum,x| sum + x }%></span>
                    <%count = total_items.inject(0){|sum,x| sum + x }%>
                <% else %>
                    <li class="nav-item"><%= link_to 'Mon panier 0', cart_path(current_user.cart.id), class: "nav-link" %></li>
                <% end %>
              </ul>
              <%end%>
    </div>
    </div>
    </nav>
    <main class="">
      <%= yield %>
    </main>
  	<footer class="footer mt-auto py-3 text-center bg-light">
        <div class="row">
          <div class="col-lg-12">
            <p>Made by <a href="https://github.com/T-PRAT">Titouan Prat</a>.</p>
            <p>Based on <a href="https://getbootstrap.com/" rel="nofollow">Bootstrap</a> and <a href="https://bootswatch.com/minty/">bootswatch minty theme</a></p>
          </div>
        </div>
    </footer>
  </body>
</html>
